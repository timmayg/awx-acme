---
- name: Create a new ACME account with Let's Encrypt Staging
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Generate an RSA Private Key
      community.crypto.openssl_privatekey:
        path: "/var/lib/awx/projects/local/private.key"

